# Populated automatically by `azd up` (postprovision hook writes src/.env).
# Copy to .env and fill values if you provision resources manually.
PROJECT_ENDPOINT=https://<account>.services.ai.azure.com/api/projects/<project>
AGENT_NAME=product-expert-agent

# Reference values for the Foundry IQ knowledge-base setup (used in the portal):
MODEL_DEPLOYMENT_NAME=gpt-4o
EMBEDDING_DEPLOYMENT_NAME=text-embedding-3-small
SEARCH_SERVICE_NAME=
SEARCH_ENDPOINT=
STORAGE_ACCOUNT_NAME=
BLOB_CONTAINER_NAME=product-data
